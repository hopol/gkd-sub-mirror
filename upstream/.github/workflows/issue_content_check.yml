name: issue_content_check

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

permissions:
  actions: write
  contents: read
  issues: write

env:
  GH_REPO: ${{ github.repository }}

jobs:
  # ── 分析节点：Python 只运行一次，输出所有原子化标志 ──

  analyze:
    name: 分析 Issue 快照链接
    runs-on: ubuntu-latest
    # 过滤逻辑：
    # 1. 白名单：仅分析通过指定模板创建的 Issue（含 `✅ 请核对以下内容` 标记）
    # 2. 通用豁免：!bot 标签（黑名单，维护者手动豁免）
    # 3. issues 事件：跳过已关闭的 Issue
    # 4. issue_comment 事件：仅处理 Issue 作者本人的评论
    if: >-
      github.event.issue.body != null &&
      contains(github.event.issue.body, '✅ 请核对以下内容') &&
      !contains(github.event.issue.labels.*.name, '!bot') &&
      (github.event_name != 'issues' || github.event.issue.state != 'CLOSED') &&
      (github.event_name != 'issue_comment' || github.event.comment.user.login == github.event.issue.user.login)
    outputs:
      has_snapshot: ${{ steps.analyze.outputs.has_snapshot }}
      has_unreachable: ${{ steps.analyze.outputs.has_unreachable }}
      network_status: ${{ steps.analyze.outputs.network_status }}
      network_detail: ${{ steps.analyze.outputs.network_detail }}
      has_convertible: ${{ steps.analyze.outputs.has_convertible }}
      warning_type: ${{ steps.analyze.outputs.warning_type }}
      comment_missing: ${{ steps.analyze.outputs.comment_missing }}
      comment_unreachable: ${{ steps.analyze.outputs.comment_unreachable }}
      comment_404: ${{ steps.analyze.outputs.comment_404 }}
      comment_uncertain: ${{ steps.analyze.outputs.comment_uncertain }}
      comment_recovery: ${{ steps.analyze.outputs.comment_recovery }}
      comment_bot: ${{ steps.analyze.outputs.comment_bot }}
      cache_data: ${{ steps.export-cache.outputs.cache_data }}
    steps:
      - name: 预检查是否需要分析（仅 issue_comment 事件）
        id: pre-check
        if: github.event_name == 'issue_comment'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          state=$(gh issue view "$ISSUE_NUMBER" --json state -q .state)
          labels=$(gh issue view "$ISSUE_NUMBER" --json labels -q '.labels[].name')
          needs="false"
          [ "$state" = "CLOSED" ] && needs="true"
          for label in $labels; do
            case "$label" in
              "缺失快照(no-snapshot)"|"需补充链接(needs-link)"|"链接失效(broken-link)")
                needs="true" ;;
            esac
          done
          echo "needs_analysis=$needs" >> "$GITHUB_OUTPUT"

      - name: 检出代码仓库
        uses: actions/checkout@v7

      - name: 配置 Python 运行环境
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: 安装UV
        uses: astral-sh/setup-uv@v8.0.0
        with:
          enable-cache: true
          cache-dependency-glob: scripts/python/requirements.txt

      - name: 安装 Python 依赖
        run: uv pip install --system -r scripts/python/requirements.txt

      - name: 创建工作流所需标签
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh label create "缺失快照(no-snapshot)" --color "#fd1b4d" --description "Issue缺少快照链接" --force
          gh label create "需补充链接(needs-link)" --color "#581598" --description "Issue包含不可访问的快照链接" --force
          gh label create "链接失效(broken-link)" --color "#12f540" --description "Issue中的链接无法访问" --force
          gh label create "!bot" --color "#000000" --description "豁免Bot自动化分析" --force

      # issue_comment 事件时，获取旧 Bot 评论作为历史链接来源
      - name: 查找旧 Bot 评论
        id: find-old-bot
        if: github.event_name == 'issue_comment'
        uses: peter-evans/find-comment@v4
        with:
          issue-number: ${{ github.event.issue.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '<!-- gkd-bot-comment -->'

      - name: 获取历史链接
        id: get-history
        if: github.event_name == 'issue_comment'
        env:
          OLD_BOT_CONTENT: ${{ steps.find-old-bot.outputs.comment-body }}
        run: |
          if [ -n "$OLD_BOT_CONTENT" ]; then
            echo "history_source=old_bot" >> "$GITHUB_OUTPUT"
            echo "history_content<<HISTORY_EOF" >> "$GITHUB_OUTPUT"
            echo "$OLD_BOT_CONTENT" >> "$GITHUB_OUTPUT"
            echo "HISTORY_EOF" >> "$GITHUB_OUTPUT"
          else
            echo "history_source=" >> "$GITHUB_OUTPUT"
            echo "history_content=" >> "$GITHUB_OUTPUT"
          fi

      # 恢复快照缓存（所有事件都可读取）
      - name: 恢复快照缓存
        uses: actions/cache/restore@v6
        with:
          path: /tmp/snapshot_cache
          key: snapshots-${{ github.event.issue.number }}
          restore-keys: |
            snapshots-

      - name: 分析 Issue 快照链接
        id: analyze
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_COMMENT_BODY: ${{ github.event.comment.body || '' }}
          ISSUE_USER: ${{ github.event.issue.user.login }}
          ISSUE_LABELS: ${{ join(github.event.issue.labels.*.name, ',') }}
          ISSUE_ACTION: ${{ github.event_name == 'issue_comment' && 'comment' || github.event.action }}
          SKIP_ANALYSIS: ${{ github.event_name == 'issue_comment' && false }}
          HISTORY_CONTENT: ${{ steps.get-history.outputs.history_content || '' }}
          HISTORY_SOURCE: ${{ steps.get-history.outputs.history_source || '' }}
        run: |
          if [ "$SKIP_ANALYSIS" = "true" ]; then
            cat >> "$GITHUB_OUTPUT" <<EOF
          has_snapshot=true
          has_unreachable=false
          network_status=ok
          network_detail=
          has_convertible=false
          warning_type=
          comment_missing=
          comment_unreachable=
          comment_404=
          comment_uncertain=
          comment_recovery=
          comment_bot=
          EOF
          else
            python scripts/python/entry/check_issue.py
          fi

      # 导出缓存数据到 job output（base64 编码避免 heredoc 大内容解析失败）
      - name: 导出缓存数据
        id: export-cache
        if: github.event_name == 'issues'
        run: |
          if [ -f /tmp/snapshot_cache/snapshots.json ]; then
            echo "cache_data=$(base64 -w 0 /tmp/snapshot_cache/snapshots.json)" >> "$GITHUB_OUTPUT"
          fi

  # ── 缓存持久化：独立 job，仅此 job 需要 actions: write ──

  save-cache:
    name: 保存快照缓存
    needs: analyze
    if: >-
      always() &&
      github.event_name == 'issues' &&
      needs.analyze.result == 'success'
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - name: 还原缓存文件
        if: needs.analyze.outputs.cache_data != ''
        run: |
          mkdir -p /tmp/snapshot_cache
          echo "${{ needs.analyze.outputs.cache_data }}" | base64 -d > /tmp/snapshot_cache/snapshots.json

      - name: 检查缓存文件是否存在
        id: check-cache
        run: |
          if [ -f /tmp/snapshot_cache/snapshots.json ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: 保存到 GitHub Actions 缓存
        if: steps.check-cache.outputs.exists == 'true'
        uses: actions/cache/save@v6
        with:
          path: /tmp/snapshot_cache
          key: snapshots-${{ github.event.issue.number }}

  # ── 线性步骤一：缺失快照 → 打标签 + 评论 + 关闭（致命，阻断后续所有 Job） ──

  handle-missing-snapshot:
    name: 处理缺失快照
    needs: analyze
    if: needs.analyze.outputs.has_snapshot == 'false'
    runs-on: ubuntu-latest
    steps:
      - name: 打标签「缺失快照」
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: gh issue edit "$ISSUE_NUMBER" --add-label "缺失快照(no-snapshot)"

      - name: 查找已有警告评论
        id: find-warning
        uses: peter-evans/find-comment@v4
        with:
          issue-number: ${{ github.event.issue.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '<!-- gkd-warning-missing -->'

      - name: 发布缺失快照警告评论
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ steps.find-warning.outputs.comment-id }}
          issue-number: ${{ github.event.issue.number }}
          body: ${{ needs.analyze.outputs.comment_missing }}
          edit-mode: replace

      - name: 关闭 Issue（缺失快照）
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: gh issue close "$ISSUE_NUMBER" --reason "not planned"

  # ── 线性步骤二：本地快照链接 → 打标签 + 评论（非致命，不阻断后续） ──

  handle-unreachable-snapshot:
    name: 处理本地快照链接
    needs: [analyze, handle-missing-snapshot]
    if: >-
      always() &&
      needs.handle-missing-snapshot.result == 'skipped' &&
      needs.analyze.outputs.has_unreachable == 'true' &&
      needs.analyze.outputs.warning_type != 'recovery'
    runs-on: ubuntu-latest
    steps:
      - name: 打标签「需补充链接」
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: gh issue edit "$ISSUE_NUMBER" --add-label "需补充链接(needs-link)"

      - name: 查找已有警告评论
        id: find-warning
        uses: peter-evans/find-comment@v4
        with:
          issue-number: ${{ github.event.issue.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '<!-- gkd-warning-unreachable -->'

      - name: 发布本地快照提醒评论
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ steps.find-warning.outputs.comment-id }}
          issue-number: ${{ github.event.issue.number }}
          body: ${{ needs.analyze.outputs.comment_unreachable }}
          edit-mode: replace

  # ── 分支步骤三：链接不可访问（404）→ 打标签 + 评论（非致命，阻断转换） ──

  handle-network-404:
    name: 处理链接不可访问(404)
    needs: [analyze, handle-missing-snapshot]
    if: >-
      always() &&
      needs.handle-missing-snapshot.result == 'skipped' &&
      needs.analyze.outputs.network_status == '404'
    runs-on: ubuntu-latest
    steps:
      - name: 打标签「链接无法访问」
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: gh issue edit "$ISSUE_NUMBER" --add-label "链接失效(broken-link)"

      - name: 查找已有警告评论
        id: find-warning
        uses: peter-evans/find-comment@v4
        with:
          issue-number: ${{ github.event.issue.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '<!-- gkd-warning-404 -->'

      - name: 发布链接不可访问提醒评论
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ steps.find-warning.outputs.comment-id }}
          issue-number: ${{ github.event.issue.number }}
          body: ${{ needs.analyze.outputs.comment_404 }}
          edit-mode: replace

  # ── 分支步骤三：网络不确定（403/5xx）→ 打标签 + 折叠评论（非致命，阻断转换） ──

  handle-network-uncertain:
    name: 处理网络不确定(403/5xx)
    needs: [analyze, handle-missing-snapshot]
    if: >-
      always() &&
      needs.handle-missing-snapshot.result == 'skipped' &&
      needs.analyze.outputs.network_status == 'uncertain'
    runs-on: ubuntu-latest
    steps:
      - name: 打标签「链接无法访问」（不确定）
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: gh issue edit "$ISSUE_NUMBER" --add-label "链接失效(broken-link)"

      - name: 查找已有警告评论
        id: find-warning
        uses: peter-evans/find-comment@v4
        with:
          issue-number: ${{ github.event.issue.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '<!-- gkd-warning-uncertain -->'

      - name: 发布网络不确定提醒评论
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ steps.find-warning.outputs.comment-id }}
          issue-number: ${{ github.event.issue.number }}
          body: ${{ needs.analyze.outputs.comment_uncertain }}
          edit-mode: replace

  # ── 步骤四：链接转换 + Bot 评论（仅当缺失未触发 + 有可转换链接时） ──

  handle-convert:
    name: 处理链接转换
    needs: [analyze, handle-missing-snapshot]
    if: >-
      always() &&
      needs.handle-missing-snapshot.result == 'skipped' &&
      needs.analyze.outputs.has_convertible == 'true'
    runs-on: ubuntu-latest
    steps:
      # issue_comment 事件时，隐藏旧 Bot 评论（折叠为 OUTDATED）
      - name: 获取旧 Bot 评论的 Node ID
        if: github.event_name == 'issue_comment'
        id: get-old-bot-node
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_OWNER: ${{ github.repository_owner }}
          REPO_NAME: ${{ github.event.repository.name }}
          ISSUE_NUM: ${{ github.event.issue.number }}
        run: |
          node_id=$(gh api graphql -f query='
          query($owner: String!, $name: String!, $number: Int!) {
            repository(owner: $owner, name: $name) {
              issue(number: $number) {
                comments(first: 100) {
                  nodes {
                    id
                    body
                    author { login }
                  }
                }
              }
            }
          }' -f owner="$REPO_OWNER" -f name="$REPO_NAME" -F number="$ISSUE_NUM" --jq '.data.repository.issue.comments.nodes[] | select(.author.login == "github-actions") | select(.body | contains("gkd-bot-comment")) | .id' 2>/dev/null | head -1)
          echo "node_id=$node_id" >> "$GITHUB_OUTPUT"

      - name: 隐藏旧 Bot 评论
        if: github.event_name == 'issue_comment' && steps.get-old-bot-node.outputs.node_id != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NODE_ID: ${{ steps.get-old-bot-node.outputs.node_id }}
        run: |
          gh api graphql -f query='
          mutation MinimizeComment($subjectId: ID!, $classifier: ReportedContentClassifiers!) {
            minimizeComment(input: { subjectId: $subjectId, classifier: $classifier }) {
              minimizedComment {
                isMinimized
                minimizedReason
              }
            }
          }' -f subjectId="$NODE_ID" -f classifier="OUTDATED"

      - name: 发布快照转换评论
        uses: peter-evans/create-or-update-comment@v5
        with:
          issue-number: ${{ github.event.issue.number }}
          body: ${{ needs.analyze.outputs.comment_bot }}

  # ── 步骤五：编辑/评论恢复（仅当缺失未触发 + 所有检查通过时） ──

  handle-recovery:
    name: 处理编辑/评论后恢复issue状态
    needs:
      [
        analyze,
        handle-missing-snapshot,
        handle-unreachable-snapshot,
        handle-network-404,
        handle-network-uncertain,
        handle-convert,
      ]
    if: >-
      always() &&
      needs.handle-missing-snapshot.result == 'skipped' &&
      needs.analyze.outputs.warning_type == 'recovery'
    runs-on: ubuntu-latest
    steps:
      - name: 移除所有本工作流管理的标签
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          for label in \
            "缺失快照(no-snapshot)" \
            "需补充链接(needs-link)" \
            "链接失效(broken-link)"; do
            gh issue edit "$ISSUE_NUMBER" --remove-label "$label" 2>/dev/null || true
          done

      - name: 重新打开 Issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: gh issue reopen "$ISSUE_NUMBER" || true

      - name: 查找已有警告评论
        id: find-warning
        uses: peter-evans/find-comment@v4
        with:
          issue-number: ${{ github.event.issue.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '<!-- gkd-warning'

      - name: 发布恢复评论
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ steps.find-warning.outputs.comment-id }}
          issue-number: ${{ github.event.issue.number }}
          body: ${{ needs.analyze.outputs.comment_recovery }}
          edit-mode: replace

      - name: 清理残留警告评论
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          comment_ids=$(gh api "repos/$GH_REPO/issues/$ISSUE_NUMBER/comments" \
            --jq '.[] | select(.user.login == "github-actions[bot]") | select(.body | test("gkd-warning")) | select(.body | test("gkd-warning-recovery") | not) | .id')
          for id in $comment_ids; do
            gh api "repos/$GH_REPO/issues/comments/$id" -X DELETE 2>/dev/null || true
          done
